через all собирается базовая версия, с музыкой  - disco (нужен SDL2_mixer)
проверить: pkg-config --modversion sdl2     
проверить: pkg-config --modversion SDL2_mixer    
установить sudo apt install libsdl2-mixer-dev
brew install sdl2 sdl2_mixer
на WSL disco может подвисать - говорят тяжело некоторым компам две звуковые системы одновременно тянуть.

ctrl + изменить масштаб терминала (строки а значит и поле станет большего размера визуально)

с эпилепсией disco не включать!

10 скоростей - увеличивается после каждых 600 очков.
очки за 1 строку - 100, 2 - 300, 3 - 700, 4 - 1500.
disco при окончании песни принудительно завершает игру. 
стрелка вниз - "сброс" прям катапультирование вниз до самого упора (а не на одну клетку).
вращение фигуры пробелом (а не стрелкой вверх).